dist
build
node_modules
pnpm-lock.yaml

# Hand-authored governance docs/templates: leave their existing formatting
# alone for now rather than reformatting unrelated content in this PR.
*.md
.github/ISSUE_TEMPLATE/

# No prettier-plugin-astro configured yet — ESLint already skips .astro files
# (see eslint.config.js) for the same reason.
*.astro
